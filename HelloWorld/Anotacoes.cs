namespace HelloWorld;

// single line comment

/* 
    multi line comment
    add as many lines as you want
 */

// we'll see more about solutions during the course

/*
Public permite que qualquer classe que tenha uma instância dessa classe acesse seus membros. 
Private restringe o acesso apenas à própria classe. 
Internal permite o acesso apenas a classes dentro do mesmo projeto. 
Protected será explicado em um contexto futuro.
 */

// class - define a classe
// se a classe não tiver um modificador de acesso, ela é internal por padrão

/*
class FirstHelloWorld {
    // void = no return value
    // console application starts from *Main* method
    static void Main()
    {
        Console.WriteLine("Hello, World!");
        
        Console.WriteLine("This is my first program in C#.");
    }
}
 */